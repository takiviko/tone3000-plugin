// `UiTestbed --selftest`: juce::UnitTest cases for the UI's pure logic.
#pragma once

namespace t3k::ui::testbed {

// Runs every "ui" unit test; 0 when all pass.
int runSelfTests();

}  // namespace t3k::ui::testbed
